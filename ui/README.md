# frontend code for ticket_booking_system

Designing a simple ui for the Ticket Booking Service
Framework: Vite.js

## running the code

Run `npm install` to install the dependencies.
Run `npm run dev` to start the development server.
